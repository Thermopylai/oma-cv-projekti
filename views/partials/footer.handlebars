<footer class="mt-5 py-4 border-top border-primary">
    <div class="container">
        <div class="row align-items-center">
            <div class="col-md-6 text-center text-md-start">
                <p class="mb-0">&copy; {{#if vuosi}}{{vuosi}}{{else}}2026{{/if}} - <strong>{{nimi}}</strong></p>
                <small class="text-muted small">Rakennettu Node.js:llä ja Handlebarsilla</small>
            </div>
            <div class="col-md-6 text-center text-md-end mt-3 mt-md-0">
                <a href="{{yhteystiedot.github}}" target="_blank" class="btn btn-outline-info btn-sm me-2">
                    <i class="bi bi-github"></i>
                </a>
                <a href="{{yhteystiedot.linkedin}}" target="_blank" class="btn btn-outline-info btn-sm me-2">
                    <i class="bi bi-linkedin"></i>
                </a>
                <a href="mailto:{{yhteystiedot.sahkoposti}}" class="btn btn-outline-info btn-sm">
                    <i class="bi bi-envelope-fill"></i>
                </a>
            </div>
        </div>
    </div>
</footer>